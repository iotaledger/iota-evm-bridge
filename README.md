# IOTA EVM Bridge


Build the sdk:
```sh
pnpm sdk build
```

Run the bride app:
```sh
pnpm app dev
```

Check eslint and formatting:

```sh
pnpm run lint
```

Fix eslint and formatting:

```sh
pnpm run lint:fix
```
